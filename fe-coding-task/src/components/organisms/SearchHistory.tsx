import React from "react";
import { Box } from "@mui/material";

export const SearchHistory: React.FC = () => {
  // Fetch search history from local storage and render
  return (
    <Box mt={2}>
      {/* Render search history */}
      <div>Search history will be rendered here</div>
    </Box>
  );
};
